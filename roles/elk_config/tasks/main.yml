---
# tasks file for setup_ELK
- include: elasticsearch.yml
- include: logstash.yml
- include: kibana.yml
- include: metricbeat.yml
- include: filebeat.yml
- include: grafana.yml
- name: Get my public IP
  ipify_facts:
  register: public_ip
- name: public IP Address
#  debug: msg="If all went well you can now connect to *** KIBANA:  http://{{ ipify_public_ip }}:5601 *** & GRAFANA: http://{{ ipify_public_ip }}:3000 *** Kibana can take a few minutes to run the first time."
  debug: msg="{{ public_ip }}"
